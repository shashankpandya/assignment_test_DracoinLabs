{
  "name": "certificate-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "CertificateRegistry smart contract for the Student Management System",
  "scripts": {
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "hardhat": "^2.22.10"
  }
}
